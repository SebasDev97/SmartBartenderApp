# Raspberry Pi only. Installed on top of requirements.txt when running with --gpio.
# Both are imported lazily inside GpioBackend, so the service starts fine without them.
gpiozero==2.0.1
rpi-lgpio==0.6
rpi_ws281x==5.0.0
