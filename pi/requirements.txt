# Core service. Runs anywhere, including a Mac with no hardware attached.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
PyYAML==6.0.2
pyserial==3.5  # the USB link to the Arduino; unused under --simulate

# Development / tests
pytest==8.3.4
httpx==0.28.1
