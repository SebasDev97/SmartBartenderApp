<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Launch theme: matches the Compose dark palette so there is no white flash on start. -->
    <style name="Theme.SmartBartender" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/obsidian</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
